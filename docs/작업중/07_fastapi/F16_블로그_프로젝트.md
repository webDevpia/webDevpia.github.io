---
title: 16. 통합 프로젝트 — 블로그 API
layout: default
grand_parent: Language
parent: FastAPI (리뉴얼)
nav_order: 16
permalink: /language/fastapi-new/blog-project
---

{% raw %}

## 학습 목표

- 지금까지 배운 모든 개념을 하나의 프로젝트에 통합할 수 있다
- 사용자 인증이 있는 블로그 API를 처음부터 끝까지 직접 구현할 수 있다
- 관계형 데이터 모델(User → Post → Comment)을 설계할 수 있다
- 소유권 검사(내 게시물만 수정/삭제)를 구현할 수 있다
- Docker로 완성된 API를 컨테이너화할 수 있다

<a id="toc"></a>

## 진행 순서

1. [프로젝트 소개](#1) — 무엇을 만드는가
2. [기술 스택 매핑](#2) — 각 챕터의 개념이 어디에 쓰이나
3. [프로젝트 구조](#3) — 파일 레이아웃
4. [Step 1 — 환경 설정](#4) — 설치, 설정, 초기화
5. [Step 2 — 모델과 스키마](#5) — User, Post, Comment
6. [Step 3 — 인증 라우터](#6) — 회원가입, 로그인, 내 정보
7. [Step 4 — 게시물 CRUD](#7) — 생성, 조회, 수정, 삭제 + 소유권
8. [Step 5 — 댓글](#8) — 게시물 하위 리소스
9. [Step 6 — 페이지네이션](#9) — skip + limit
10. [Step 7 — 테스트](#10) — 핵심 흐름 5가지
11. [Step 8 — Docker](#11) — 컨테이너화 및 실행
12. [개념 매핑표](#12) — 전체 요약
13. [과정을 마치며](#13) — 회고 + 다음 단계

---

# 16장. 통합 프로젝트 — 블로그 API + Docker

이 장은 앞에서 배운 모든 것을 하나로 합치는 **최종 프로젝트**입니다. 완성된 ToDo API를 발판으로, 이번에는 사용자 인증, 게시물 CRUD, 댓글 기능까지 갖춘 블로그 API를 처음부터 구현합니다.

---

<a id="1"></a>

## 1️⃣ 프로젝트 소개 [↑](#toc)

### 만들 것: 블로그 API

| 기능 | 엔드포인트 | 인증 필요 |
|------|------------|-----------|
| 회원가입 | `POST /auth/register` | 불필요 |
| 로그인 | `POST /auth/token` | 불필요 |
| 내 정보 | `GET /auth/me` | 필요 |
| 게시물 목록 | `GET /posts` | 불필요 (공개) |
| 게시물 단건 | `GET /posts/{id}` | 불필요 (공개) |
| 게시물 생성 | `POST /posts` | 필요 |
| 게시물 수정 | `PATCH /posts/{id}` | 필요 (작성자만) |
| 게시물 삭제 | `DELETE /posts/{id}` | 필요 (작성자만) |
| 내 게시물 목록 | `GET /posts/mine` | 필요 |
| 댓글 목록 | `GET /posts/{id}/comments` | 불필요 |
| 댓글 작성 | `POST /posts/{id}/comments` | 필요 |
| 댓글 삭제 | `DELETE /comments/{id}` | 필요 (작성자만) |

### 핵심 규칙

- **누구나** 게시물과 댓글을 읽을 수 있습니다 (공개 블로그)
- **로그인한 사용자만** 게시물과 댓글을 작성할 수 있습니다
- **자신이 작성한 것만** 수정하거나 삭제할 수 있습니다

---

<a id="2"></a>

## 2️⃣ 기술 스택 매핑 [↑](#toc)

이 프로젝트에서 각 챕터에서 배운 개념이 어떻게 활용되는지 확인해보세요.

| 챕터 | 핵심 개념 | 이 프로젝트에서의 역할 |
|------|-----------|----------------------|
| 03 | Path / Query 파라미터 | `GET /posts/{id}`, `?skip=0&limit=10` 페이지네이션 |
| 04 | Pydantic v2 | 요청/응답 스키마 (`PostCreate`, `PostResponse`) |
| 05 | response_model, 상태 코드 | 각 엔드포인트 응답 타입 명시 |
| 06 | CRUD 패턴 | posts, comments 전체 CRUD 구현 |
| 07 | APIRouter | `/auth`, `/posts`, `/comments` 라우터 분리 |
| 08 | Depends | 세션, 현재 사용자 의존성 주입 |
| 09 | 에러 처리 | 404 (없음), 401 (미인증), 403 (권한 없음), 422 (검증 실패) |
| 11 | SQLModel 관계 | `User → Post → Comment` 1:N 관계 |
| 13 | 관계 쿼리 | `select(Post).where(Post.user_id == user.id)` |
| 14 | JWT 인증 | 로그인, 보호된 라우트, 소유권 확인 |
| 15 | pydantic-settings | `.env`에서 SECRET_KEY, DATABASE_URL 읽기 |
| 15 | pytest + TestClient | 핵심 API 흐름 자동 테스트 |

---

<a id="3"></a>

## 3️⃣ 프로젝트 구조 [↑](#toc)

```
blog-api/
├── app/
│   ├── main.py              # FastAPI 앱 생성, 라우터 등록
│   ├── config.py            # pydantic-settings (환경 변수)
│   ├── database.py          # 엔진, 세션, get_session
│   ├── dependencies.py      # get_current_user (공통 의존성)
│   ├── exceptions.py        # 공통 예외 함수
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── post.py
│   │   └── comment.py
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── post.py
│   │   └── comment.py
│   └── routers/
│       ├── __init__.py
│       ├── auth.py
│       ├── posts.py
│       └── comments.py
├── tests/
│   ├── conftest.py
│   ├── test_auth.py
│   └── test_posts.py
├── .env                     # 비밀값 (gitignore)
├── .env.example             # 형식 예시 (공유 가능)
├── .gitignore
├── Dockerfile
└── requirements.txt
```

---

<a id="4"></a>

## 4️⃣ Step 1 — 환경 설정 [↑](#toc)

### 프로젝트 초기화

```bash
mkdir blog-api && cd blog-api
uv init
uv add fastapi "uvicorn[standard]" sqlmodel "pwdlib[argon2]" PyJWT "pydantic[email]" pydantic-settings
uv add --dev pytest httpx pytest-cov
```

### .env 파일

```bash
# .env
DATABASE_URL=sqlite:///blog.db
SECRET_KEY=여기에-openssl-rand--hex-32-결과를-붙여넣으세요
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256
```

### app/config.py

```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    database_url: str = "sqlite:///blog.db"
    secret_key: str
    access_token_expire_minutes: int = 30
    algorithm: str = "HS256"

    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
    )


settings = Settings()
```

### app/database.py

```python
from sqlmodel import SQLModel, Session, create_engine
from app.config import settings

engine = create_engine(settings.database_url)


def create_db_and_tables():
    SQLModel.metadata.create_all(engine)


def get_session():
    with Session(engine) as session:
        yield session
```

### app/main.py

```python
from contextlib import asynccontextmanager
from fastapi import FastAPI

from app.database import create_db_and_tables
from app.routers import auth, posts, comments


@asynccontextmanager
async def lifespan(app: FastAPI):
    create_db_and_tables()
    yield


app = FastAPI(
    title="블로그 API",
    description="FastAPI 과정 최종 프로젝트",
    version="1.0.0",
    lifespan=lifespan,
)

app.include_router(auth.router)
app.include_router(posts.router)
app.include_router(comments.router)


@app.get("/")
def root():
    return {"message": "블로그 API에 오신 것을 환영합니다"}
```

---

<a id="5"></a>

## 5️⃣ Step 2 — 모델과 스키마 [↑](#toc)

### 데이터 모델 관계

```
User (1) ─────── (N) Post (1) ─────── (N) Comment
                           └─────────────────────┘
                              Comment.post_id = Post.id
                              Comment.user_id = User.id
```

### app/models/user.py

```python
from typing import Optional, TYPE_CHECKING
from sqlmodel import SQLModel, Field, Relationship

if TYPE_CHECKING:
    from app.models.post import Post
    from app.models.comment import Comment


class User(SQLModel, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    email: str = Field(unique=True, index=True)
    hashed_password: str
    is_active: bool = Field(default=True)

    posts: list["Post"] = Relationship(back_populates="author")
    comments: list["Comment"] = Relationship(back_populates="author")
```

### app/models/post.py

```python
from typing import Optional, TYPE_CHECKING
from datetime import datetime, timezone
from sqlmodel import SQLModel, Field, Relationship

if TYPE_CHECKING:
    from app.models.user import User
    from app.models.comment import Comment


class Post(SQLModel, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    title: str = Field(min_length=1, max_length=200)
    content: str
    created_at: datetime = Field(
        default_factory=lambda: datetime.now(timezone.utc)
    )
    updated_at: Optional[datetime] = Field(default=None)

    user_id: Optional[int] = Field(default=None, foreign_key="user.id")
    author: Optional["User"] = Relationship(back_populates="posts")
    comments: list["Comment"] = Relationship(back_populates="post")
```

### app/models/comment.py

```python
from typing import Optional, TYPE_CHECKING
from datetime import datetime, timezone
from sqlmodel import SQLModel, Field, Relationship

if TYPE_CHECKING:
    from app.models.user import User
    from app.models.post import Post


class Comment(SQLModel, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    content: str = Field(min_length=1)
    created_at: datetime = Field(
        default_factory=lambda: datetime.now(timezone.utc)
    )

    user_id: Optional[int] = Field(default=None, foreign_key="user.id")
    post_id: Optional[int] = Field(default=None, foreign_key="post.id")

    author: Optional["User"] = Relationship(back_populates="comments")
    post: Optional["Post"] = Relationship(back_populates="comments")
```

### app/schemas/post.py

```python
from pydantic import BaseModel, ConfigDict
from datetime import datetime
from typing import Optional


class PostCreate(BaseModel):
    title: str
    content: str


class PostUpdate(BaseModel):
    title: Optional[str] = None
    content: Optional[str] = None


class PostResponse(BaseModel):
    id: int
    title: str
    content: str
    created_at: datetime
    user_id: int

    model_config = ConfigDict(from_attributes=True)


class PostDetailResponse(PostResponse):
    comment_count: int = 0
```

### app/schemas/comment.py

```python
from pydantic import BaseModel, ConfigDict
from datetime import datetime


class CommentCreate(BaseModel):
    content: str


class CommentResponse(BaseModel):
    id: int
    content: str
    created_at: datetime
    user_id: int
    post_id: int

    model_config = ConfigDict(from_attributes=True)
```

---

<a id="6"></a>

## 6️⃣ Step 3 — 인증 라우터 [↑](#toc)

### app/dependencies.py — 공통 의존성

```python
import jwt
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from jwt.exceptions import InvalidTokenError
from sqlmodel import Session, select

from app.config import settings
from app.database import get_session
from app.models.user import User

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/auth/token")


def get_current_user(
    token: str = Depends(oauth2_scheme),
    session: Session = Depends(get_session),
) -> User:
    credentials_exception = HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="인증 정보를 확인할 수 없습니다.",
        headers={"WWW-Authenticate": "Bearer"},
    )

    try:
        payload = jwt.decode(
            token, settings.secret_key, algorithms=[settings.algorithm]
        )
        email: str | None = payload.get("sub")
        if email is None:
            raise credentials_exception
    except InvalidTokenError:
        raise credentials_exception

    user = session.exec(select(User).where(User.email == email)).first()
    if user is None:
        raise credentials_exception

    return user
```

### app/routers/auth.py

```python
import jwt
from datetime import datetime, timedelta, timezone
from fastapi import APIRouter, HTTPException, Depends, status
from fastapi.security import OAuth2PasswordRequestForm
from pwdlib import PasswordHash
from sqlmodel import Session, select

from app.config import settings
from app.database import get_session
from app.dependencies import get_current_user
from app.models.user import User
from app.schemas.user import UserCreate, UserResponse, Token

router = APIRouter(prefix="/auth", tags=["인증"])
password_hash = PasswordHash.recommended()


def create_access_token(data: dict) -> str:
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(
        minutes=settings.access_token_expire_minutes
    )
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, settings.secret_key, algorithm=settings.algorithm)


@router.post("/register", response_model=UserResponse, status_code=status.HTTP_201_CREATED)
def register(user_data: UserCreate, session: Session = Depends(get_session)):
    existing = session.exec(select(User).where(User.email == user_data.email)).first()
    if existing:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="이미 등록된 이메일입니다.",
        )

    new_user = User(
        email=user_data.email,
        hashed_password=password_hash.hash(user_data.password),
    )
    session.add(new_user)
    session.commit()
    session.refresh(new_user)
    return new_user


@router.post("/token", response_model=Token)
def login(
    form_data: OAuth2PasswordRequestForm = Depends(),
    session: Session = Depends(get_session),
):
    user = session.exec(select(User).where(User.email == form_data.username)).first()

    if not user or not password_hash.verify(form_data.password, user.hashed_password):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="이메일 또는 비밀번호가 올바르지 않습니다.",
            headers={"WWW-Authenticate": "Bearer"},
        )

    token = create_access_token({"sub": user.email})
    return Token(access_token=token)


@router.get("/me", response_model=UserResponse)
def get_me(current_user: User = Depends(get_current_user)):
    return current_user
```

---

<a id="7"></a>

## 7️⃣ Step 4 — 게시물 CRUD [↑](#toc)

### 소유권 검사 패턴

> 소유권 검사 = 자물쇠가 있는 사물함 — 열쇠를 가진 사람(작성자)만 열 수 있습니다.

```python
def check_post_owner(post: Post, current_user: User) -> None:
    if post.user_id != current_user.id:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="이 게시물을 수정/삭제할 권한이 없습니다.",
        )
```

### app/routers/posts.py

```python
from datetime import datetime, timezone
from typing import Optional
from fastapi import APIRouter, HTTPException, Depends, status
from sqlmodel import Session, select

from app.database import get_session
from app.dependencies import get_current_user
from app.models.post import Post
from app.models.user import User
from app.schemas.post import PostCreate, PostUpdate, PostResponse

router = APIRouter(prefix="/posts", tags=["게시물"])


def get_post_or_404(post_id: int, session: Session) -> Post:
    post = session.get(Post, post_id)
    if not post:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"게시물 {post_id}를 찾을 수 없습니다.",
        )
    return post


@router.get("/", response_model=list[PostResponse])
def read_posts(
    skip: int = 0,
    limit: int = 10,
    session: Session = Depends(get_session),
):
    posts = session.exec(
        select(Post).offset(skip).limit(limit).order_by(Post.created_at.desc())
    ).all()
    return posts


@router.get("/mine", response_model=list[PostResponse])
def read_my_posts(
    skip: int = 0,
    limit: int = 10,
    session: Session = Depends(get_session),
    current_user: User = Depends(get_current_user),
):
    posts = session.exec(
        select(Post)
        .where(Post.user_id == current_user.id)
        .offset(skip)
        .limit(limit)
        .order_by(Post.created_at.desc())
    ).all()
    return posts


@router.get("/{post_id}", response_model=PostResponse)
def read_post(post_id: int, session: Session = Depends(get_session)):
    return get_post_or_404(post_id, session)


@router.post("/", response_model=PostResponse, status_code=status.HTTP_201_CREATED)
def create_post(
    post_data: PostCreate,
    session: Session = Depends(get_session),
    current_user: User = Depends(get_current_user),
):
    new_post = Post(**post_data.model_dump(), user_id=current_user.id)
    session.add(new_post)
    session.commit()
    session.refresh(new_post)
    return new_post


@router.patch("/{post_id}", response_model=PostResponse)
def update_post(
    post_id: int,
    post_data: PostUpdate,
    session: Session = Depends(get_session),
    current_user: User = Depends(get_current_user),
):
    post = get_post_or_404(post_id, session)

    if post.user_id != current_user.id:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="이 게시물을 수정할 권한이 없습니다.",
        )

    update_data = post_data.model_dump(exclude_unset=True)
    post.sqlmodel_update(update_data)
    post.updated_at = datetime.now(timezone.utc)

    session.add(post)
    session.commit()
    session.refresh(post)
    return post


@router.delete("/{post_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_post(
    post_id: int,
    session: Session = Depends(get_session),
    current_user: User = Depends(get_current_user),
):
    post = get_post_or_404(post_id, session)

    if post.user_id != current_user.id:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="이 게시물을 삭제할 권한이 없습니다.",
        )

    session.delete(post)
    session.commit()
```

---

<a id="8"></a>

## 8️⃣ Step 5 — 댓글 [↑](#toc)

### app/routers/comments.py

```python
from fastapi import APIRouter, HTTPException, Depends, status
from sqlmodel import Session, select

from app.database import get_session
from app.dependencies import get_current_user
from app.models.comment import Comment
from app.models.post import Post
from app.models.user import User
from app.schemas.comment import CommentCreate, CommentResponse

router = APIRouter(tags=["댓글"])


@router.get("/posts/{post_id}/comments", response_model=list[CommentResponse])
def read_comments(post_id: int, session: Session = Depends(get_session)):
    post = session.get(Post, post_id)
    if not post:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"게시물 {post_id}를 찾을 수 없습니다.",
        )
    comments = session.exec(
        select(Comment).where(Comment.post_id == post_id)
    ).all()
    return comments


@router.post(
    "/posts/{post_id}/comments",
    response_model=CommentResponse,
    status_code=status.HTTP_201_CREATED,
)
def create_comment(
    post_id: int,
    comment_data: CommentCreate,
    session: Session = Depends(get_session),
    current_user: User = Depends(get_current_user),
):
    post = session.get(Post, post_id)
    if not post:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"게시물 {post_id}를 찾을 수 없습니다.",
        )

    new_comment = Comment(
        **comment_data.model_dump(),
        post_id=post_id,
        user_id=current_user.id,
    )
    session.add(new_comment)
    session.commit()
    session.refresh(new_comment)
    return new_comment


@router.delete("/comments/{comment_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_comment(
    comment_id: int,
    session: Session = Depends(get_session),
    current_user: User = Depends(get_current_user),
):
    comment = session.get(Comment, comment_id)
    if not comment:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"댓글 {comment_id}를 찾을 수 없습니다.",
        )

    if comment.user_id != current_user.id:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="이 댓글을 삭제할 권한이 없습니다.",
        )

    session.delete(comment)
    session.commit()
```

---

<a id="9"></a>

## 9️⃣ Step 6 — 페이지네이션 [↑](#toc)

### 페이지네이션이란?

> 책의 목차처럼 — 전체 내용을 한꺼번에 보여주는 대신 페이지 단위로 나눠서 보여줍니다.

게시물이 10,000개라면 한 번에 모두 가져오면 느립니다. `skip`과 `limit`으로 나눠서 가져옵니다.

```
GET /posts?skip=0&limit=10    → 1~10번째 게시물
GET /posts?skip=10&limit=10   → 11~20번째 게시물
GET /posts?skip=20&limit=10   → 21~30번째 게시물
```

이미 `posts.py`의 `read_posts`에 구현되어 있습니다:

```python
@router.get("/", response_model=list[PostResponse])
def read_posts(
    skip: int = 0,
    limit: int = 10,
    session: Session = Depends(get_session),
):
    posts = session.exec(
        select(Post).offset(skip).limit(limit).order_by(Post.created_at.desc())
    ).all()
    return posts
```

| 파라미터 | SQL 변환 | 의미 |
|----------|----------|------|
| `skip=0` | `OFFSET 0` | 처음부터 시작 |
| `limit=10` | `LIMIT 10` | 최대 10개 반환 |
| `.order_by(Post.created_at.desc())` | `ORDER BY created_at DESC` | 최신순 정렬 |

---

<a id="10"></a>

## 🔟 Step 7 — 테스트 [↑](#toc)

### tests/conftest.py

```python
import pytest
from fastapi.testclient import TestClient
from sqlmodel import SQLModel, Session, create_engine
from sqlmodel.pool import StaticPool

from app.main import app
from app.database import get_session


@pytest.fixture(name="session")
def session_fixture():
    engine = create_engine(
        "sqlite://",
        connect_args={"check_same_thread": False},
        poolclass=StaticPool,
    )
    SQLModel.metadata.create_all(engine)
    with Session(engine) as session:
        yield session


@pytest.fixture(name="client")
def client_fixture(session: Session):
    def override_get_session():
        yield session

    app.dependency_overrides[get_session] = override_get_session
    client = TestClient(app)
    yield client
    app.dependency_overrides.clear()


@pytest.fixture(name="auth_client")
def auth_client_fixture(client: TestClient):
    """로그인된 클라이언트를 반환하는 픽스처"""
    client.post(
        "/auth/register",
        json={"email": "author@example.com", "password": "테스트비밀번호123"},
    )
    login_response = client.post(
        "/auth/token",
        data={"username": "author@example.com", "password": "테스트비밀번호123"},
    )
    token = login_response.json()["access_token"]
    client.headers.update({"Authorization": f"Bearer {token}"})
    return client
```

### tests/test_posts.py — 핵심 5가지 테스트

```python
from fastapi.testclient import TestClient


def test_create_and_read_post(auth_client: TestClient):
    """케이스 1: 게시물 생성 후 조회"""
    create_resp = auth_client.post(
        "/posts/",
        json={"title": "첫 번째 글", "content": "내용입니다"},
    )
    assert create_resp.status_code == 201
    post_id = create_resp.json()["id"]

    read_resp = auth_client.get(f"/posts/{post_id}")
    assert read_resp.status_code == 200
    assert read_resp.json()["title"] == "첫 번째 글"


def test_public_read_without_auth(client: TestClient, auth_client: TestClient):
    """케이스 2: 비로그인 사용자도 게시물 읽기 가능"""
    auth_client.post(
        "/posts/",
        json={"title": "공개 게시물", "content": "누구나 볼 수 있습니다"},
    )

    read_resp = client.get("/posts/")
    assert read_resp.status_code == 200
    assert len(read_resp.json()) >= 1


def test_update_own_post(auth_client: TestClient):
    """케이스 3: 자신의 게시물 수정"""
    create_resp = auth_client.post(
        "/posts/",
        json={"title": "원래 제목", "content": "원래 내용"},
    )
    post_id = create_resp.json()["id"]

    update_resp = auth_client.patch(
        f"/posts/{post_id}",
        json={"title": "수정된 제목"},
    )
    assert update_resp.status_code == 200
    assert update_resp.json()["title"] == "수정된 제목"
    assert update_resp.json()["content"] == "원래 내용"  # content는 유지


def test_cannot_delete_others_post(client: TestClient, auth_client: TestClient):
    """케이스 4: 다른 사람의 게시물 삭제 불가"""
    # auth_client (author)가 게시물 생성
    create_resp = auth_client.post(
        "/posts/",
        json={"title": "내 게시물", "content": "내용"},
    )
    post_id = create_resp.json()["id"]

    # 다른 사용자 등록 후 로그인
    client.post(
        "/auth/register",
        json={"email": "other@example.com", "password": "비밀번호456"},
    )
    login_resp = client.post(
        "/auth/token",
        data={"username": "other@example.com", "password": "비밀번호456"},
    )
    other_token = login_resp.json()["access_token"]

    # 다른 사용자가 삭제 시도 → 403
    delete_resp = client.delete(
        f"/posts/{post_id}",
        headers={"Authorization": f"Bearer {other_token}"},
    )
    assert delete_resp.status_code == 403


def test_create_and_read_comment(auth_client: TestClient):
    """케이스 5: 댓글 생성 및 조회"""
    post_resp = auth_client.post(
        "/posts/",
        json={"title": "댓글 테스트 글", "content": "내용"},
    )
    post_id = post_resp.json()["id"]

    comment_resp = auth_client.post(
        f"/posts/{post_id}/comments",
        json={"content": "좋은 글이네요!"},
    )
    assert comment_resp.status_code == 201

    list_resp = auth_client.get(f"/posts/{post_id}/comments")
    assert list_resp.status_code == 200
    assert len(list_resp.json()) == 1
    assert list_resp.json()[0]["content"] == "좋은 글이네요!"
```

### 전체 테스트 실행

```bash
pytest -v
```

실행 결과:
```
====================== test session starts ======================
collected 5 items

tests/test_posts.py::test_create_and_read_post PASSED      [20%]
tests/test_posts.py::test_public_read_without_auth PASSED  [40%]
tests/test_posts.py::test_update_own_post PASSED           [60%]
tests/test_posts.py::test_cannot_delete_others_post PASSED [80%]
tests/test_posts.py::test_create_and_read_comment PASSED  [100%]

======================= 5 passed in 2.14s =======================
```

---

<a id="11"></a>

## 1️⃣1️⃣ Step 8 — Docker [↑](#toc)

### Docker란?

> Docker = 이사박스 — 앱과 앱이 필요한 모든 것(Python, 패키지, 설정)을 하나의 박스에 담습니다.
> 어떤 서버에서든 같은 박스를 열면 동일하게 실행됩니다.

### requirements.txt 생성

```bash
uv export --no-dev --no-hashes -o requirements.txt
```

### Dockerfile

```dockerfile
FROM python:3.12-slim

# 작업 디렉토리 설정
WORKDIR /code

# 의존성 먼저 복사 (레이어 캐시 활용)
COPY ./requirements.txt /code/requirements.txt
RUN pip install --no-cache-dir -r /code/requirements.txt

# 앱 코드 복사
COPY ./app /code/app

# FastAPI 실행
CMD ["fastapi", "run", "app/main.py", "--port", "80"]
```

### 이미지 빌드

```bash
docker build -t blog-api .
```

실행 결과:
```
[+] Building 23.4s (10/10) FINISHED
 => [1/5] FROM docker.io/library/python:3.12-slim
 => [2/5] WORKDIR /code
 => [3/5] COPY requirements.txt /code/requirements.txt
 => [4/5] RUN pip install -r /code/requirements.txt
 => [5/5] COPY ./app /code/app
 => exporting to image
Successfully built 4a2b3c1d5e6f
Successfully tagged blog-api:latest
```

### 컨테이너 실행

```bash
docker run -p 8000:80 --env-file .env blog-api
```

`http://localhost:8000/docs`에 접속하면 완성된 블로그 API의 Swagger UI가 나타납니다.

> `--env-file .env`로 `.env` 파일의 환경변수를 컨테이너에 전달합니다.
> `SECRET_KEY` 같은 민감한 값이 Dockerfile에 포함되지 않습니다.

### .gitignore 최종 확인

```bash
# .gitignore
.env
*.db
__pycache__/
.pytest_cache/
.venv/
```

---

<a id="12"></a>

## 1️⃣2️⃣ 개념 매핑표 [↑](#toc)

이 과정에서 배운 모든 개념이 최종 프로젝트에 어떻게 활용되었는지 정리합니다.

| 챕터 | 핵심 개념 | 코드 위치 | 역할 |
|------|-----------|-----------|------|
| 01 | HTTP, REST | — | 전체 설계 철학 |
| 02 | FastAPI 설치, Swagger UI | `main.py` | 앱 초기화, 자동 문서화 |
| 03 | Path / Query 파라미터 | `posts.py` `GET /posts/{id}`, `?skip=0&limit=10` | URL 파라미터, 페이지네이션 |
| 04 | Pydantic BaseModel | `schemas/*.py` | 모든 요청/응답 스키마 |
| 05 | response_model, 상태 코드 | 모든 라우터 | 타입 안전한 응답 |
| 06 | CRUD 패턴 | `posts.py`, `comments.py` | 전체 CRUD 구현 |
| 07 | APIRouter, prefix | `routers/` | `/auth`, `/posts`, `/comments` 분리 |
| 08 | Depends | `get_session`, `get_current_user` | 세션, 인증 의존성 주입 |
| 09 | HTTPException | `get_post_or_404`, 소유권 검사 | 404, 401, 403 에러 처리 |
| 10 | lifespan | `main.py` | DB 테이블 자동 생성 |
| 11 | SQLModel, Field | `models/*.py` | 테이블 정의 |
| 12 | Relationship, foreign_key | `models/*.py` | User→Post→Comment 관계 |
| 13 | select, where, offset | `posts.py`, `comments.py` | DB 쿼리 |
| 14 | JWT, pwdlib, OAuth2 | `auth.py`, `dependencies.py` | 인증 전체 흐름 |
| 15 | pydantic-settings, .env | `config.py` | 환경변수 관리 |
| 15 | pytest, TestClient, conftest | `tests/` | 자동화 테스트 |
| 16 | Docker | `Dockerfile` | 컨테이너 배포 |

---

<a id="13"></a>

## 1️⃣3️⃣ 과정을 마치며 [↑](#toc)

### 01장에서 16장까지 — 비교

**01장에서 처음 만든 코드:**

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def read_root():
    return {"Hello": "World"}
```

3줄이었습니다. 웹 API가 무엇인지, HTTP가 무엇인지 막막하게 시작했던 그 코드입니다.

**16장의 블로그 API:**

```
300줄 이상의 프로덕션 수준 API
├── JWT 인증 (14장)
├── 비밀번호 해싱 (14장)
├── 관계형 DB (11~13장)
├── 의존성 주입 (08장)
├── 에러 처리 (09장)
├── 자동화 테스트 (15장)
├── 환경 변수 관리 (15장)
└── Docker 배포 (16장)
```

> "01장에서 '웹 API란 식당 웨이터'라고 배운 것이 이제 실제 API가 되었습니다."
> 음식 주문을 받고, 주방(DB)에 전달하고, 결과를 손님(클라이언트)에게 돌려주는 웨이터가 완성되었습니다.

### 이제 할 수 있는 것들

이 과정을 완료한 여러분은 다음을 직접 만들 수 있습니다:

- 사용자 인증이 있는 REST API
- SQLite 또는 PostgreSQL 기반의 데이터 저장/조회
- 관계형 데이터 모델 설계 (1:N, M:N)
- pytest로 API 엔드포인트 자동 테스트
- Docker로 API 컨테이너화

### 다음으로 배우면 좋은 것들

| 주제 | 이유 |
|------|------|
| **Async SQLAlchemy** | SQLModel의 비동기 버전으로 성능 향상 |
| **Alembic** | DB 스키마 변경을 코드로 관리 (마이그레이션) |
| **Redis** | 세션, 캐시, 속도 제한 구현 |
| **WebSocket** | 실시간 채팅, 알림 기능 |
| **React / Next.js 프론트엔드** | 이 API를 실제 UI와 연결 |
| **GitHub Actions CI/CD** | 코드 push 시 자동 테스트 + 배포 |
| **클라우드 배포** | AWS, GCP, Railway, Fly.io에 배포 |
| **PostgreSQL** | 프로덕션 수준의 DB로 전환 |

### 마지막으로

```
처음에는 "웹 API가 뭔지"도 몰랐습니다.
이제 JWT 인증이 있는 블로그 API를 Docker로 배포할 수 있습니다.

작은 차이가 쌓여 큰 능력이 됩니다.
이 과정을 완주한 것을 진심으로 축하합니다.
```

### 실습 과제

**기본**: 이 장의 코드를 처음부터 직접 작성하고 `pytest -v`로 5가지 테스트가 모두 통과하는지 확인해보세요.

**중급**: `GET /posts`에 `title` 검색 기능을 추가해보세요. `?title=FastAPI`처럼 쿼리 파라미터로 제목에 특정 단어가 포함된 게시물만 반환합니다. SQLModel의 `where(Post.title.contains(keyword))`를 활용해보세요.

**심화**: `GET /posts/{post_id}`의 응답에 댓글 수(`comment_count`)를 포함해보세요. `PostDetailResponse` 스키마를 활용하고, `session.exec(select(func.count()).where(...))` 방식으로 댓글 수를 쿼리합니다. 이후 Docker로 빌드하고 `docker run`으로 실행하여 `http://localhost:8000/docs`에서 전체 API를 확인해보세요.

{% endraw %}
