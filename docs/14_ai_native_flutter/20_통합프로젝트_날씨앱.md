---
title: "19. 통합 프로젝트 — 날씨 앱"
layout: default
parent: AI-Native Flutter
nav_order: 21
permalink: /ai-native-flutter/weather-app
---

# 19장. 통합 프로젝트 — 날씨 앱
{: .no_toc }

> **Phase 3** · 예상 시간: 핵심 프로젝트

> 💡 **실습 흐름**: 강사 시연 보기 → 함께 따라하기 → 혼자 변형해 보기

> **이 장의 AI 사용 규칙 — 완전 페어 프로그래밍**
> AI(Copilot)와 완전한 협업이 가능합니다.
> 내가 설계하고 AI가 구현합니다. 테스트로 검증합니다.
> "신뢰하되 검증한다(Trust but verify)"가 원칙입니다.

## 학습 목표

- 전체 AI-Native 워크플로우(요구사항 → Instructions → TDD → 구현 → 검증)를 수행할 수 있다
- OpenWeatherMap API와 연동하는 Flutter 앱을 만들 수 있다
- `SharedPreferences`로 검색 기록을 로컬에 저장할 수 있다

<a id="toc"></a>
## 진행 순서

1. [프로젝트 소개](#part1) — 무엇을 만드나요?
2. [Step 1: 요구사항 작성](#part2) — 설계 먼저
3. [Step 2: 프로젝트 구조 + Instructions](#part3) — 뼈대 세우기
4. [Step 3: OpenWeatherMap API 키 발급](#part4) — API 준비
5. [Step 4: TDD로 핵심 로직 구현](#part5) — 테스트 먼저
6. [Step 5: UI 구현](#part6) — 화면 만들기
7. [Step 6: 통합 테스트 + 디버깅](#part7) — 연결하고 확인하기
8. [최종 산출물 확인](#part8) — 완성 체크리스트
9. [마무리](#part9)

---

<a id="part1"></a>
## 1️⃣ 프로젝트 소개 [↑](#toc)

### 무엇을 만드나요?

도시 이름을 입력하면 현재 날씨 정보를 보여주는 Flutter 앱입니다.

**주요 기능:**

1. **날씨 검색**: 도시 이름(한국어/영어) 입력 후 검색
2. **날씨 정보**: 온도(°C), 날씨 설명, 날씨 아이콘
3. **에러 처리**: 잘못된 도시명, 네트워크 오류, 빈 검색어 안내
4. **검색 기록**: 최근 5개 도시를 SharedPreferences에 저장

### 사용할 기술

| 기술 | 용도 |
|------|------|
| Flutter / Dart | 앱 개발 |
| http 패키지 | API 호출 |
| shared_preferences | 검색 기록 저장 |
| OpenWeatherMap API | 날씨 데이터 |
| flutter_test | 테스트 |
| GitHub Copilot | AI 협업 |

### AI-Native 도구 전체

```
.github/
├── copilot-instructions.md        ← 16장에서 만든 것
├── instructions/
│   ├── flutter.instructions.md   ← 16장에서 만든 것
│   └── testing.instructions.md   ← 16장에서 만든 것
└── prompts/
    ├── new-screen.prompt.md       ← 16장에서 만든 것
    └── write-tests.prompt.md      ← 16장에서 만든 것
```

---

<a id="part2"></a>
## 2️⃣ ⭐ Step 1: 요구사항 작성 [↑](#toc)

코드를 짜기 전에 **요구사항**을 먼저 정리합니다.

### 기능 요구사항

```
날씨 앱 요구사항
================

[검색 기능]
- 도시명 입력 후 검색 버튼 또는 키보드 확인 버튼으로 검색
- 한국어 도시명 지원 (서울, 부산, 제주 등)
- 영어 도시명 지원 (Seoul, Tokyo, New York 등)
- 빈 검색어 입력 시 "도시명을 입력해주세요" 메시지 표시

[날씨 정보 표시]
- 도시명 (API 응답에서 받은 이름)
- 현재 온도 (°C, 소수점 없이 반올림)
- 날씨 설명 (예: 맑음, 구름 많음)
- 날씨 아이콘 (OpenWeatherMap 아이콘 이미지)
- 체감 온도, 최고/최저 온도

[에러 처리]
- 존재하지 않는 도시: "도시를 찾을 수 없습니다" 메시지
- 네트워크 오류: "인터넷 연결을 확인해주세요" 메시지
- 에러 화면에서 다시 시도 버튼 제공

[검색 기록]
- 성공한 검색의 도시명을 로컬에 저장
- 최대 5개 유지 (초과 시 가장 오래된 것 제거)
- 앱 재시작 후에도 유지 (SharedPreferences)
- 검색 기록 탭으로 빠른 재검색 가능
- 검색 기록 전체 삭제 기능
```

### Copilot Chat에 요구사항 전달

```
위 요구사항을 읽고, 구현에 필요한 파일 구조와 클래스 목록을 제안해줘.
단, 아직 코드를 작성하지는 말고 계획만 알려줘.
```

---

<a id="part3"></a>
## 3️⃣ ⭐ Step 2: 프로젝트 구조 + Instructions [↑](#toc)

### 프로젝트 생성

```bash
flutter create weather_app
cd weather_app
flutter pub add http shared_preferences
```

### 파일 구조

```
weather_app/
├── .github/
│   ├── copilot-instructions.md
│   ├── instructions/
│   │   ├── flutter.instructions.md
│   │   └── testing.instructions.md
│   └── prompts/
│       └── write-tests.prompt.md
├── lib/
│   ├── main.dart
│   ├── models/
│   │   └── weather.dart
│   ├── services/
│   │   ├── weather_service.dart
│   │   └── history_service.dart
│   ├── screens/
│   │   └── weather_screen.dart
│   └── widgets/
│       ├── weather_display.dart
│       └── history_list.dart
└── test/
    ├── models/
    │   └── weather_test.dart
    └── services/
        ├── weather_service_test.dart
        └── history_service_test.dart
```

### copilot-instructions.md에 추가할 내용

```markdown
## 프로젝트: 날씨 앱 (weather_app)

### 사용 패키지
- http: ^1.2.0
- shared_preferences: ^2.2.2

### API
- OpenWeatherMap Current Weather API
- Base URL: https://api.openweathermap.org/data/2.5
- Units: metric (섭씨)
- Lang: kr (한국어 날씨 설명)

### 모델: Weather
- cityName, temperature, feelsLike, tempMin, tempMax: double
- description, iconCode: String
- iconUrl: getter (https://openweathermap.org/img/wn/{iconCode}@2x.png)
- temperatureRounded: getter (반올림 정수)
```

---

<a id="part4"></a>
## 4️⃣ ⭐ Step 3: OpenWeatherMap API 키 발급 [↑](#toc)

1. [openweathermap.org](https://openweathermap.org) 접속
2. "Sign up" → 무료 계정 생성 및 이메일 인증
3. 로그인 후 "My API Keys" 탭에서 키 복사
4. 활성화까지 최대 2시간 소요

### API 테스트

```bash
curl "https://api.openweathermap.org/data/2.5/weather?q=Seoul&appid=YOUR_KEY&units=metric&lang=kr"
```

### API 응답 예시

```json
{
  "name": "Seoul",
  "main": {
    "temp": 18.5,
    "feels_like": 17.2,
    "temp_min": 16.0,
    "temp_max": 21.0
  },
  "weather": [
    {
      "description": "맑음",
      "icon": "01d"
    }
  ]
}
```

---

<a id="part5"></a>
## 5️⃣ ⭐ Step 4: TDD로 핵심 로직 구현 [↑](#toc)

### 4-1. Weather 모델 테스트 (Red)

```dart
// test/models/weather_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:weather_app/models/weather.dart';

void main() {
  group('Weather', () {
    const sampleJson = {
      'name': 'Seoul',
      'main': {
        'temp': 18.5,
        'feels_like': 17.2,
        'temp_min': 16.0,
        'temp_max': 21.0,
      },
      'weather': [
        {'description': '맑음', 'icon': '01d'}
      ],
    };

    test('fromJson()으로 올바르게 생성된다', () {
      final weather = Weather.fromJson(sampleJson);
      expect(weather.cityName, equals('Seoul'));
      expect(weather.temperature, equals(18.5));
      expect(weather.description, equals('맑음'));
      expect(weather.iconCode, equals('01d'));
    });

    test('iconUrl이 올바른 형식이다', () {
      final weather = Weather.fromJson(sampleJson);
      expect(
        weather.iconUrl,
        equals('https://openweathermap.org/img/wn/01d@2x.png'),
      );
    });

    test('temperatureRounded가 반올림된 값을 반환한다', () {
      final weather = Weather.fromJson(sampleJson);
      expect(weather.temperatureRounded, equals(19));
    });
  });
}
```

Copilot 요청:

```
위 테스트를 모두 통과하는 Weather 클래스를 lib/models/weather.dart에 작성해줘.
fromJson() factory 생성자, iconUrl getter, temperatureRounded getter 포함.
```

### 4-2. Weather 모델 구현 (Green)

```dart
// lib/models/weather.dart
class Weather {
  final String cityName;
  final double temperature;
  final double feelsLike;
  final double tempMin;
  final double tempMax;
  final String description;
  final String iconCode;

  const Weather({
    required this.cityName,
    required this.temperature,
    required this.feelsLike,
    required this.tempMin,
    required this.tempMax,
    required this.description,
    required this.iconCode,
  });

  factory Weather.fromJson(Map<String, dynamic> json) {
    final main = json['main'] as Map<String, dynamic>;
    final weatherList = json['weather'] as List<dynamic>;
    final weatherInfo = weatherList.first as Map<String, dynamic>;

    return Weather(
      cityName: json['name'] as String,
      temperature: (main['temp'] as num).toDouble(),
      feelsLike: (main['feels_like'] as num).toDouble(),
      tempMin: (main['temp_min'] as num).toDouble(),
      tempMax: (main['temp_max'] as num).toDouble(),
      description: weatherInfo['description'] as String,
      iconCode: weatherInfo['icon'] as String,
    );
  }

  String get iconUrl =>
      'https://openweathermap.org/img/wn/$iconCode@2x.png';

  int get temperatureRounded => temperature.round();
}
```

테스트 실행: `flutter test test/models/weather_test.dart`

### 4-3. WeatherService 테스트

```dart
// test/services/weather_service_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:weather_app/services/weather_service.dart';

void main() {
  group('WeatherService', () {
    late WeatherService service;

    setUp(() {
      service = WeatherService(apiKey: 'test-key');
    });

    test('buildUrl()이 올바른 URL을 생성한다', () {
      final url = service.buildUrl('Seoul');
      expect(url, contains('Seoul'));
      expect(url, contains('test-key'));
      expect(url, contains('metric'));
      expect(url, contains('lang=kr'));
    });

    test('parseError()가 404일 때 적절한 메시지를 반환한다', () {
      final message = service.parseError(404);
      expect(message, contains('찾을 수 없'));
    });

    test('parseError()가 401일 때 API 키 오류 메시지를 반환한다', () {
      final message = service.parseError(401);
      expect(message, contains('API 키'));
    });

    test('fetchWeather()가 빈 도시명이면 ArgumentError를 발생시킨다', () {
      expect(
        () => service.fetchWeather(''),
        throwsArgumentError,
      );
    });
  });
}
```

### 4-4. WeatherService 구현

```dart
// lib/services/weather_service.dart
import 'package:http/http.dart' as http;
import 'dart:convert';
import '../models/weather.dart';

class WeatherService {
  static const String _baseUrl = 'https://api.openweathermap.org/data/2.5';
  final String apiKey;

  WeatherService({required this.apiKey});

  String buildUrl(String city) {
    return '$_baseUrl/weather?q=$city&appid=$apiKey&units=metric&lang=kr';
  }

  String parseError(int statusCode) {
    switch (statusCode) {
      case 404:
        return '도시를 찾을 수 없습니다. 도시명을 다시 확인해주세요.';
      case 401:
        return 'API 키가 올바르지 않습니다.';
      case 429:
        return '요청이 너무 많습니다. 잠시 후 다시 시도해주세요.';
      default:
        return '날씨 정보를 가져오지 못했습니다. (코드: $statusCode)';
    }
  }

  Future<Weather> fetchWeather(String city) async {
    if (city.trim().isEmpty) {
      throw ArgumentError('도시명을 입력해주세요.');
    }

    try {
      final url = Uri.parse(buildUrl(city.trim()));
      final response = await http.get(url);

      if (response.statusCode == 200) {
        final json = jsonDecode(response.body) as Map<String, dynamic>;
        return Weather.fromJson(json);
      } else {
        throw Exception(parseError(response.statusCode));
      }
    } on http.ClientException {
      throw Exception('인터넷 연결을 확인해주세요.');
    }
  }
}
```

### 4-5. HistoryService (SharedPreferences)

```dart
// lib/services/history_service.dart
import 'package:shared_preferences/shared_preferences.dart';

class HistoryService {
  static const String _key = 'search_history';
  static const int _maxHistory = 5;

  Future<List<String>> getHistory() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getStringList(_key) ?? [];
  }

  Future<void> addCity(String city) async {
    final prefs = await SharedPreferences.getInstance();
    final history = prefs.getStringList(_key) ?? [];

    history.remove(city);
    history.insert(0, city);
    if (history.length > _maxHistory) {
      history.removeRange(_maxHistory, history.length);
    }

    await prefs.setStringList(_key, history);
  }

  Future<void> clearHistory() async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.remove(_key);
  }
}
```

---

<a id="part6"></a>
## 6️⃣ ⭐ Step 5: UI 구현 [↑](#toc)

### 6-1. 메인 화면 (WeatherScreen)

```dart
// lib/screens/weather_screen.dart
import 'package:flutter/material.dart';
import '../models/weather.dart';
import '../services/weather_service.dart';
import '../services/history_service.dart';

class WeatherScreen extends StatefulWidget {
  const WeatherScreen({super.key});

  @override
  State<WeatherScreen> createState() => _WeatherScreenState();
}

class _WeatherScreenState extends State<WeatherScreen> {
  final WeatherService _weatherService = WeatherService(
    apiKey: 'YOUR_API_KEY_HERE', // 발급받은 API 키 입력
  );
  final HistoryService _historyService = HistoryService();
  final TextEditingController _controller = TextEditingController();

  Future<Weather>? _weatherFuture;
  List<String> _history = [];

  @override
  void initState() {
    super.initState();
    _loadHistory();
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }

  Future<void> _loadHistory() async {
    final history = await _historyService.getHistory();
    if (mounted) {
      setState(() => _history = history);
    }
  }

  void _search(String city) {
    if (city.trim().isEmpty) {
      ScaffoldMessenger.of(context).showSnackBar(
        const SnackBar(content: Text('도시명을 입력해주세요')),
      );
      return;
    }

    setState(() {
      _weatherFuture = _weatherService.fetchWeather(city).then((weather) async {
        await _historyService.addCity(city.trim());
        await _loadHistory();
        return weather;
      });
    });

    FocusScope.of(context).unfocus();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('날씨 앱'),
        backgroundColor: Theme.of(context).colorScheme.inversePrimary,
        actions: [
          if (_history.isNotEmpty)
            IconButton(
              icon: const Icon(Icons.delete_sweep),
              onPressed: () async {
                await _historyService.clearHistory();
                await _loadHistory();
              },
              tooltip: '검색 기록 삭제',
            ),
        ],
      ),
      body: Column(
        children: [
          // 검색 바
          Padding(
            padding: const EdgeInsets.all(16),
            child: TextField(
              controller: _controller,
              decoration: InputDecoration(
                hintText: '도시명 입력 (예: Seoul, 서울)',
                prefixIcon: const Icon(Icons.search),
                suffixIcon: IconButton(
                  icon: const Icon(Icons.send),
                  onPressed: () => _search(_controller.text),
                ),
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(12),
                ),
              ),
              onSubmitted: _search,
              textInputAction: TextInputAction.search,
            ),
          ),

          // 검색 기록
          if (_history.isNotEmpty)
            SizedBox(
              height: 40,
              child: ListView.builder(
                scrollDirection: Axis.horizontal,
                padding: const EdgeInsets.symmetric(horizontal: 12),
                itemCount: _history.length,
                itemBuilder: (context, index) {
                  return Padding(
                    padding: const EdgeInsets.only(right: 8),
                    child: ActionChip(
                      label: Text(_history[index]),
                      onPressed: () {
                        _controller.text = _history[index];
                        _search(_history[index]);
                      },
                    ),
                  );
                },
              ),
            ),

          const SizedBox(height: 8),

          // 날씨 정보 영역
          Expanded(
            child: _weatherFuture == null
                ? const Center(
                    child: Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      children: [
                        Icon(Icons.wb_sunny_outlined, size: 80, color: Colors.amber),
                        SizedBox(height: 16),
                        Text(
                          '도시명을 검색해보세요',
                          style: TextStyle(fontSize: 18, color: Colors.grey),
                        ),
                      ],
                    ),
                  )
                : FutureBuilder<Weather>(
                    future: _weatherFuture,
                    builder: (context, snapshot) {
                      if (snapshot.connectionState == ConnectionState.waiting) {
                        return const Center(child: CircularProgressIndicator());
                      }
                      if (snapshot.hasError) {
                        return Center(
                          child: Column(
                            mainAxisAlignment: MainAxisAlignment.center,
                            children: [
                              const Icon(Icons.error_outline,
                                  size: 64, color: Colors.red),
                              const SizedBox(height: 16),
                              Text(
                                '${snapshot.error}'.replaceAll('Exception: ', ''),
                                textAlign: TextAlign.center,
                                style: const TextStyle(fontSize: 16),
                              ),
                              const SizedBox(height: 16),
                              ElevatedButton.icon(
                                onPressed: () => _search(_controller.text),
                                icon: const Icon(Icons.refresh),
                                label: const Text('다시 시도'),
                              ),
                            ],
                          ),
                        );
                      }
                      final weather = snapshot.data!;
                      return _WeatherDisplay(weather: weather);
                    },
                  ),
          ),
        ],
      ),
    );
  }
}

class _WeatherDisplay extends StatelessWidget {
  final Weather weather;
  const _WeatherDisplay({required this.weather});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      padding: const EdgeInsets.all(24),
      child: Column(
        children: [
          Text(
            weather.cityName,
            style: Theme.of(context).textTheme.headlineMedium?.copyWith(
                  fontWeight: FontWeight.bold,
                ),
          ),
          const SizedBox(height: 8),
          Image.network(
            weather.iconUrl,
            width: 100,
            height: 100,
            errorBuilder: (_, __, ___) =>
                const Icon(Icons.wb_cloudy, size: 100, color: Colors.blue),
          ),
          Text(
            '${weather.temperatureRounded}°C',
            style: Theme.of(context).textTheme.displayMedium?.copyWith(
                  fontWeight: FontWeight.bold,
                  color: Colors.blue,
                ),
          ),
          Text(
            weather.description,
            style: Theme.of(context).textTheme.titleLarge,
          ),
          const SizedBox(height: 24),
          Card(
            child: Padding(
              padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 16),
              child: Row(
                mainAxisAlignment: MainAxisAlignment.spaceAround,
                children: [
                  _InfoTile(
                      label: '체감', value: '${weather.feelsLike.round()}°'),
                  _InfoTile(
                      label: '최저', value: '${weather.tempMin.round()}°'),
                  _InfoTile(
                      label: '최고', value: '${weather.tempMax.round()}°'),
                ],
              ),
            ),
          ),
        ],
      ),
    );
  }
}

class _InfoTile extends StatelessWidget {
  final String label;
  final String value;
  const _InfoTile({required this.label, required this.value});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text(label, style: const TextStyle(color: Colors.grey)),
        const SizedBox(height: 4),
        Text(value,
            style: const TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
      ],
    );
  }
}
```

### 6-2. main.dart

```dart
// lib/main.dart
import 'package:flutter/material.dart';
import 'screens/weather_screen.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: '날씨 앱',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
        useMaterial3: true,
      ),
      home: const WeatherScreen(),
    );
  }
}
```

---

<a id="part7"></a>
## 7️⃣ 🚀 Step 6: 통합 테스트 + 디버깅 [↑](#toc)

### 확인 시나리오

다음 시나리오를 직접 실행해서 확인합니다:

| 시나리오 | 예상 결과 |
|---------|---------|
| "Seoul" 검색 | 서울 날씨 표시, 검색 기록에 추가 |
| "서울" 검색 | 서울 날씨 표시 |
| "asdfghjkl" 검색 | "도시를 찾을 수 없습니다" 에러 |
| 빈 칸으로 검색 | SnackBar: "도시명을 입력해주세요" |
| 검색 기록 칩 탭 | 해당 도시 재검색 |
| 6개 도시 검색 후 확인 | 검색 기록에 최대 5개만 유지 |
| 삭제 버튼 탭 | 검색 기록 전부 삭제 |
| 앱 재시작 | 검색 기록이 유지됨 |

### 자주 발생하는 문제

**문제 1: API 키가 아직 활성화 안 됨**
```
해결: openweathermap.org에서 발급 후 최대 2시간 대기
확인: 브라우저에서 직접 URL 테스트
```

**문제 2: Image.network 아이콘 안 보임**
```
해결: AndroidManifest.xml에 인터넷 권한 확인
<uses-permission android:name="android.permission.INTERNET" />
```

**문제 3: 비동기 후 context 사용 경고**
```
해결: mounted 체크 추가
if (mounted) {
  ScaffoldMessenger.of(context).showSnackBar(...);
}
```

---

<a id="part8"></a>
## 8️⃣ 최종 산출물 확인 [↑](#toc)

### 기능 체크리스트

- [ ] 도시명 입력 후 날씨 정보가 표시된다
- [ ] 한국어/영어 도시명 모두 지원한다
- [ ] 온도, 날씨 설명, 아이콘이 표시된다
- [ ] 체감 온도, 최고/최저 온도가 표시된다
- [ ] 잘못된 도시명 입력 시 에러 메시지가 표시된다
- [ ] 빈 검색어 입력 시 안내 메시지가 표시된다
- [ ] 검색 성공 시 도시명이 검색 기록에 추가된다
- [ ] 검색 기록은 최대 5개를 유지한다
- [ ] 앱 재시작 후에도 검색 기록이 유지된다
- [ ] 검색 기록 칩을 탭하면 재검색된다
- [ ] 검색 기록 삭제 기능이 동작한다

### 코드 품질 체크리스트

- [ ] `.github/copilot-instructions.md`가 있다
- [ ] `Weather.fromJson()` 단위 테스트가 통과한다
- [ ] `WeatherService` 단위 테스트가 통과한다
- [ ] `flutter test`를 실행하면 모든 테스트가 통과한다
- [ ] `print()`가 아닌 `debugPrint()` 또는 에러 처리를 사용한다

---

<a id="part9"></a>
## 9️⃣ 마무리 [↑](#toc)

### 이 프로젝트에서 사용한 AI-Native 워크플로우

```
요구사항 작성 (사람)
    ↓
파일 구조 설계 (사람 + AI 제안)
    ↓
.github/ Instructions 세팅 (사람)
    ↓
테스트 먼저 작성 (사람)
    ↓
구현 요청 (AI)
    ↓
테스트로 검증 (자동화)
    ↓
UI 구현 (AI + 사람 검토)
    ↓
직접 시나리오 테스트 (사람)
```

### 다음 프로젝트

다음 장에서는 **할일 관리 앱**을 만듭니다.  
이번 날씨 앱에서 배운 `SharedPreferences`를 활용해서, CRUD 기능과 필터링을 구현합니다.
