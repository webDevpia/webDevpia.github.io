name: Deploy Jekyll site to Pages

on:
  push:
    branches: ["main"]
    paths:
      - 'docs/01_git/**'
      - 'docs/02_language/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write